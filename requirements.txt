# Prefer:  pip install -e .            (engine)
#          pip install -e ".[person]"  (+ person matte)
# This file mirrors the runtime deps for environments that want a plain requirements list.
numpy>=2.0
opencv-python>=4.10
moderngl>=5.12
Pillow>=10.0
imageio>=2.34
imageio-ffmpeg>=0.5
pyobjc-framework-AVFoundation>=10.0 ; sys_platform == "darwin"

# optional extras
sounddevice>=0.4      # live mic reactivity  ([audio])
mediapipe>=0.10       # person matte          ([person])
pytest>=8.0           # tests                 ([dev])
